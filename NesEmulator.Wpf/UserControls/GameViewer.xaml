<UserControl x:Class="NesEmulator.Wpf.UserControls.GameViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NesEmulator.Wpf.UserControls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:GameViewer}"
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Button Content="Random"
                    Click="ButtonBase_OnClick_Random" />
            <Button Content="Start" 
                    Click="ButtonBase_OnClick_Start" />
        </StackPanel>

        <Image Grid.Row="1"
               Source="{Binding Bitmap}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               RenderOptions.BitmapScalingMode="NearestNeighbor" />
    </Grid>

</UserControl>